{
  "name": "OnaFlix Admin Panel",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/postgresql:1": {
      "version": "16"
    }
  },
  "forwardPorts": [5000, 5432],
  "portsAttributes": {
    "5000": {
      "label": "Flask App",
      "onAutoForward": "notify"
    },
    "5432": {
      "label": "PostgreSQL",
      "onAutoForward": "ignore"
    }
  },
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && psql -U postgres -c \"CREATE USER gitpod WITH PASSWORD 'gitpod';\" && psql -U postgres -c \"CREATE DATABASE onaflix_admin OWNER gitpod;\" && psql -U postgres -c \"GRANT ALL PRIVILEGES ON DATABASE onaflix_admin TO gitpod;\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  }
}
